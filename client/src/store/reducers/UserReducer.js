const {}= require('../types')

const iState = {
        name='',
        email='',
        password='',
    signUpSubmited: false
}

const UserReducer = (state =iState, action) => {
    switch(action.type){

    }
}

export default UserReducer